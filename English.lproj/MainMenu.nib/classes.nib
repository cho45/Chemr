{
    IBClasses = (
        {
            ACTIONS = {close = id; open = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {webview = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}